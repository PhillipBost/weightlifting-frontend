| Date of &nbsp; update | Description |
| ---------------- | ----------- |
| 9-17-2026 (22:42 EDT) | [Fixed] Standardized container width on `/admin/owlcms` to match the site-wide `max-w-[1200px]` specification (`globals.css` / `.card-primary`). Removed conflicting nested `max-w-4xl` and `max-w-5xl` constraints from `OwlcmsUploader`, `QuarantineReviewQueue`, and `AthleteReviewQueue`, and aligned the top tab navigation bar so the mini card and active content edges align flush with zero horizontal shift. |
| 9-15-2026 (21:19 EDT) | [Fixed] Standardized competition date formatting across federations (`Jun 5, 2026` vs `Apr 30, 2025`) using timezone-invariant normalization (`formatDisplayDate`). Added candidate attempt progressions (`snatch_lift_1..3`, `cj_lift_1..3`) to database queries in `GET /api/owlcms/reviews` so both OWLCMS and candidate columns display complete 3-attempt lift brackets (`[attempt 1, attempt 2, attempt 3]`). |
| 9-15-2026 (21:07 EDT) | [Changed] Refactored Athlete Review Queue from disparate nested boxes into a horizontally-aligned comparison table adhering to the site's dark palette design system (`globals.css` / `DisambiguationUI.tsx`). Every attribute (Athlete Name, Country, Demographics, Meet, Category, Snatch, Clean & Jerk, Total, and Delta) is now strictly aligned side-by-side on the exact same row. Replaced saturated pastel/neon backgrounds and rainbow badges with neutral dark slate, clean white typography, and understated action buttons. |
| 9-15-2026 (20:57 EDT) | [Changed] Upgraded Athlete Review Queue into a comprehensive Head-to-Head Athlete Dossier. Enriched `GET /api/owlcms/reviews` to return full competition results (Snatch, C&J, Total, attempts, bodyweight, category, meet name and date) for both OWLCMS lifters and suggested candidates (IWF and USAW). Standardized country representation across both sides with national flags and ISO codes (`🇨🇦 Canada (CAN)` vs `🇺🇸 USA (USA)`). Added direct comparison links (`View Athlete Profile ↗`, `Official IWF Bio ↗`, `USAW Sport80 Profile ↗`, and `View Meet Results ↗`). Added real-time lift delta metrics (`Total Δ: X kg`) and parsed evidence scoring chips for transparent manual resolution. |
| 9-15-2026 (20:05 EDT) | [Added] Implemented Granular Enrichment Audit transparency UI and API delta tracking for OWLCMS meet updates (`accept_enrich`). When updating an existing meet with a newer or richer export, the system inspects current parent records and only backfills missing fields without overwriting existing data. Displays an on-screen interactive audit report card with metric cards (Birth Dates, Memberships, Clubs, Categories), real-time athlete search, and per-lifter badge chips showing exact backfilled values. If the meet is already complete (e.g. v2 in database, older v1 uploaded), displays a zero-overwrites-needed confirmation explaining that all records are complete and uncorrupted. |
| 9-15-2026 (16:10 EDT) | [Added] Built Athlete Review Queue for ambiguous cross-federation athlete matches (`link_status = 'REVIEW_NEEDED'`). Created `GET /api/owlcms/reviews` to fetch pending candidate evaluations and `POST /api/owlcms/reviews/resolve` to persist manual resolutions directly to PostgreSQL (`link` writes pairwise alias to `public.athlete_aliases`; `reject` appends candidate ID to `rejected_candidate_ids` blacklist array and isolates). Upgraded `/admin/owlcms` into a 3-tab administrative suite: Upload Meets, Meet Review (quarantined files), and Athlete Review (borderline athlete candidates). |
| 9-15-2026 (13:55 EDT) | [Added] Upgraded `POST /api/owlcms/upload` into a standalone, universal integration endpoint. Added dual authentication supporting both browser admin sessions and pre-shared API keys (`x-api-key` / Bearer token via `OWLCMS_API_KEY`). Enabled direct raw OWLCMS JSON body ingestion alongside frontend wrappers, and decoupled the route from cross-container webhook dependencies, allowing external software (OWLCMS desktop application, creator portal, and automated scripts) to submit competition files reliably. |
| 9-14-2026 (23:35 EDT) | [Changed] Replaced technical developer jargon across the OWLCMS competition importer and review queue with clear, intuitive English. Converted checklist steps to "Backup Raw File", "Import Meet Results", and "Verify & Complete"; renamed badges and tabs to "Competition Importer", "Upload Meets", and "Review Queue"; clarified file status badges to "Imported", "Held for Review", and "Already in Database (Skipped)"; and converted administrative actions to "Update Existing Meet #X", "Import as New Meet", "Ignore & Block Future Uploads", and "Delete Upload". |
| 9-14-2026 (23:28 EDT) | [Added] Implemented pure-storage OWLCMS quarantine architecture and 4-action administrative review suite. Collisions by meet name and date now bypass all database insertion (0 rows in `owlcms_meets`, `owlcms_lifters`, and `owlcms_meet_results`), uploading compressed `.json.gz` files strictly to `quarantine/` in storage. Built Quarantine Review Queue on `/admin/owlcms` supporting 4 decisions: Reject & Delete, Reject & Keep (Hash Blocklist), Accept & Upload (New Meet), and Accept & Enrich (updating existing meets without duplicate rows). |
| 9-14-2026 (22:38 EDT) | [Fixed] Enforced strict database isolation for quarantined / pending-review OWLCMS meets in `app/api/owlcms/upload/route.ts`. When a collision or candidate revision is staged, bulk insertion into `owlcms_lifters` and `owlcms_meet_results` is completely bypassed (inserting 0 rows), preventing unreviewed records from leaking into live production tables. |
| 9-14-2026 (22:05 EDT) | [Changed] Updated `OwlcmsUploader` file queue status label to replace ambiguous "Duplicate" with "Already Exists (Skipped)", eliminating confusion with action verbs. Added color-coded badges for all terminal states (`✓ Ingested` in emerald, `Staged for Review` in amber, `Already Exists (Skipped)` in slate, and `Failed` in rose). |
| 9-14-2026 (22:00 EDT) | [Fixed] Resolved runtime `ReferenceError: INITIAL_STEPS is not defined` in `OwlcmsUploader` by declaring `INITIAL_STEPS` at module scope. Verified clean compilation with `tsc --noEmit`. |
| 9-14-2026 (21:26 EDT) | [Changed] Restructured `OwlcmsUploader` into a zero-frame-shift invariant layout. The Drag & Drop zone and Action Controls bar are now permanently anchored at the top of the container from start to finish, eliminating vertical jumping. The Action Button is always visible (disabled when 0 files, enabled when loaded). Newly added queue items default to compact single-row cards (`isExpanded: false`) with automatic step expansion during active processing, and the batch audit summary flows strictly downward underneath the controls. |
| 9-14-2026 (21:22 EDT) | [Changed] Removed redundant "Admin Scoped" pill badge from the top navigation bar of the OWLCMS Admin Importer page (`app/admin/owlcms/page.tsx`) to eliminate UI clutter. |
| 9-14-2026 (21:08 EDT) | [Changed] Streamlined `OwlcmsUploader` control bar to use a single unified button strictly bound to the user's "Dry Run (Simulation)" checkbox. When Dry Run is checked, the button displays "Simulate Ingestion"; when unchecked, it displays "Start Ingestion Queue". Following a simulation, the user simply unchecks Dry Run and clicks "Start Ingestion Queue" to commit in-memory files directly to the database without re-uploading or encountering divergent buttons. Removed all misleading hardcoded target estimates in favor of live measured execution timings. |
| 9-14-2026 (18:15 EDT) | Upgraded OWLCMS ingestion suite to support high-throughput multi-file batch queue processing and backwards-compatible ingestion of legacy pre-v2 OWLCMS JSON exports. Implemented permanent on-screen checklist persistence in `OwlcmsUploader` with sequential queue runner, automated bitwise duplicate skipping, and non-destructive semantic collision staging (`status = 'pending_review'`) linked to `parent_meet_id` for administrative data enrichment (e.g. recovering missing birthdates and federations). Added live pending review counter endpoint (`/api/owlcms/pending-count`) and animated glowing notification indicators on the front-page and admin dashboard navigation cards. |
| 9-14-2026 (16:35 EDT) | Added 3-tier duplicate collision detection and staged revision curation workflow for OWLCMS imports. Implemented pre-flight check API (`/api/owlcms/check`) and client-side pre-check in `OwlcmsUploader`: Tier 1 blocks exact bitwise duplicates (409 Conflict); Tier 2 detects semantic matches (`meet_name` + `start_date`) and presents an amber Revision Advisory card with optional context notes textarea, safely staging submissions as `status = 'pending_review'` linked to `parent_meet_id` without corrupting public records; Tier 3 publishes clean new meets directly. Added migration script `migrations/add_owlcms_revision_and_status_columns.sql`. |
| 9-14-2026 (15:11 EDT) | Optimized OWLCMS upload ingestion from ~18s down to sub-second (< 1s) by eliminating sequential $N+1$ per-athlete database lookups and replacing them with single-batch bulk inserts for `owlcms_lifters` and `owlcms_meet_results`. Added real-time 3-step visual checklist to `OwlcmsUploader` UI with high-resolution server execution metrics (`timings.compression_and_storage_ms`, `timings.bulk_insert_ms`, and `timings.total_ms`). |
| 9-14-2026        | Added uploader audit tracking (`uploaded_by`, `uploader_email`, `uploader_ip`) and gzip archival integration. API route now compresses raw payloads in memory via `zlib.gzip`, uploads to private `owlcms-archives` Supabase Storage bucket (`meets/{id}/{file}.json.gz`), and persists `raw_storage_path`, byte size, and dual SHA-256 checksums (`raw_storage_hash`, `raw_payload_hash`) in `owlcms_meets`. |
| 9-13-2026        | Added OWLCMS JSONv2 Competition Importer and full analytics/sabermetrics schema parity (GAMX suite, Q-scores, bounce-back recovery metrics, attempt success counts, and YTD bests). Built drag-and-drop uploader UI (`OwlcmsUploader`), Admin page route (`/admin/owlcms`), server API route (`/api/owlcms/upload`) with automated calculation trigger compatibility, and `OwlcmsMeetResult` TypeScript interfaces. |
| 9-7-2026         | Fixed CARTO "API KEY REQUIRED" watermark on meet and club maps by introducing centralized map tile configuration supporting `NEXT_PUBLIC_CARTO_API_KEY` with automatic dark-filtered OpenStreetMap fallback. |
| 9-27-2025        | Added WSO pages |
| 9-14-2025        | Added elevation data to meets |
| 9-13-2025        | Added persistent account creation and login capabilities enabled by Supabase Auth |
| 9-8-2025         | Added Performance Profile on athlete pages. <br> Made q-youth (ages 10-20), q-points (ages 21-30), and q-masters (ages 31+) mutually exclusive categories | 
| 9-1-2025         | Created search bar for meet results. <br> - Incorporated fuzzy search features <br> Improved fuzzy search for existing athlete search. <br> - Common name shortenings now reference full names, and vice versa (eg, Chris <--> Christopher) <br> - Certain known aliases were applied for specific athletes (eg, Mattie --> Martha)
| 8-31-2025        | Created meet results pages which contain (unofficial) division rankings and links to athlete pages. Athlete pages also contain links to historical meets within their competition results table. |
| 8-30-2025 &nbsp; | Fixed chart zoom function on athlete page. Now when zoom is turned on, hover cards (which were causing issues) are disabled. |
| 8-29-2025        | Disambiguation was not showing accurate WSO & barbell club information, making it difficult to... disambiguate |
| 8-28-2025        | Added name slug URL capability. <br> ../athlete/olivia-reeves already worked, but now typing in ../athlete/olivia reeves automatically converts the URL to ../athlete/olivia%20reeves which also works. <br> This is a nice feature but it isn't flawless. Some effort & precision is required from the end user. The name has to be spelled correctly, and there are also challenges associated with hyphenated names. <br> For example, ../athlete/Mary Theisen-Lappen will get converted to functional URL ../athlete/Mary%20Theisen-Lappen. However typing in ../athlete/Mary Theisen Lappen will not work since the URL conversion logic can't know where the space should go vs. where the hyphen between last name should go. <br> For athletes with duplicate names, typing their name(s) into the URL no longer shows an 'Athlete Not Found' page. It's now an athlete disambiguation page which shows you options to choose from the athletes with the same name. |
| 8-27-2025        | - De-duplicated most of the 1000+ athletes who share names <br> - Finalized start-to-finish daily scrape workflow which now includes de-duplication automation |
| 8-17-2025        | - Removed high-contrast theme and minimized the theme switching to a small button <br> - Made all card borders consistent <br> - Added toggle buttons to both charts on Athlete page <br> - Vertical hover line added to charts on Athlete page <br> - Removed Previous button when on the first page of result table & removed Next button when on the last page of result table results <br> - Added Chart legends <br> - Included brief description of both charts|
| 8-16-2025        | - Fixed nightly division scraper to now update WSO and barbell club info <br> - Fixed issue where result tables on athlete pages gave the correct number of results above the table, but at the bottom it told you a different number (in intervals of 20) if there were more than 20 meets. <br> - Added Level (local, national, international, etc.) to expanded meet results section within athlete page. <br> - Made light theme a little less harsh on the eyes. <br> - Made athlete results tables sortable |
| 8-15-2025        | - Created Light & High-Contrast themes <br> - Fixed search to include most recent WSO & barbell club information <br> - Added former Olympians (etc) to suggested search |
| 8-14-2025        | Added GitHub link to home page and reformatted footer. |
| 8-12-2025        | Q-scores for athletes <12 in age or <40 kg bodyweight were assigned NULL q-scores since these ages and weights tend to produce wild numbers. This will be done automatically by Supabase moving forward. <br> Also removed some obvious outliers from the database. If intent behind typo was obvious, I fixed it. If I wasn't sure, value was set to NULL. |
| 8-11-2025        | Table compact/expanded button with csv export to match WSO & Club columns added to expanded results table |
| 8-10-2025        | Expanded README.md |
| 8-9-2025         | Added vercel analytics functionality & github changelog & github feature wishlist |
| 8-8-2025         | Went live on vercel hosted site   |
